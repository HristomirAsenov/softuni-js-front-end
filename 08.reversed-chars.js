function reversedChars (charA, charB, charC) {
    // console.log(charC + " " + charB + " " + charA);
    console.log(`${charC} ${charB} ${charA}`);
}

// reversedChars(); // call me (and don't forget the parameters :)
